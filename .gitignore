# Coq build (generated by coq_makefile), root only
/Makefile
/Makefile.conf
/.Makefile.d

# Coq compilation artifacts (anywhere)
*.vo
*.vok
*.vos
*.vod
*.glob
*.aux
.*.aux
.lia.cache
.nia.cache

# OCaml extraction: generated sources and build tree (regenerate with
# `make -C extraction`); the sources Extract.v / main.ml / dune* are kept.
extraction/nbe.ml
extraction/nbe.mli
extraction/_build/
